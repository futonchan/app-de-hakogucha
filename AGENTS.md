# AGENTS.md — アプリでハコグチャ（仮）

## 目的と現在地

iPhone Safari・縦持ちで遊ぶ、1人用・60秒のリアルタイムパズルMVPを実装する。
最終目標は2人対戦だが、今回は通信・対戦を作らない。
この引継ぎパックの初期状態は**仕様書だけ**であり、ゲーム本体・package.json・テストは未実装。
特定のCodexモデル名・モデルID・推論設定に依存させない。

## 読む順番

実装前に以下を読む。詳細ルールをこのファイルだけから推測しない。

1. `docs/MVP_SPEC.md` — 機能仕様。`R-xx`は承認済み、`P-xx`は未承認の初期実装案。
2. `docs/DECISIONS.md` — 承認の根拠、仮仕様、変更時の扱い。
3. `config/mvp-defaults.json` — 確定値と仮値を分離した設定資料。
4. `docs/ARCHITECTURE.md` — 状態、イベント順序、時間・入力・描画の分離。
5. `docs/ACCEPTANCE_TESTS.md` — 実装するテストと実機確認。
6. `docs/IMPLEMENTATION_PLAN.md` — 段階的な実装と完了条件。

## 仕様を扱う原則

- 現在のユーザー指示を最優先に、明示された目的・制約の範囲で作業する。
- 承認済み`R-xx`を、原作動画・Web情報・過去の助手の発言で上書きしない。
- `P-xx`は実装を進めるための仮置きであり、ユーザー承認済みと言わない。関数・設定で切り離す。
- 文書間で`R-xx`が矛盾していたら黙って解釈せず、衝突箇所を示して確認する。
- 目的・スコープ・承認値、費用、外部公開、個人情報の取扱いを変える場合は先に確認する。
- 明記済みの`P-xx`や軽微で可逆的な補完は、判断と影響を記録して進める。既に答えのある質問を繰り返さない。
- 有力な代替案があれば、効果・費用・安全性・実行可能性とトレードオフを短く説明する。推測した「本当の要望」を理由に制約を変更しない。
- 実装開始を依頼されたら、計画や仕様書の再掲だけで終了せず、動作する最小ループと検証まで進める。

## 絶対に取り違えないルール

- 盤面は10列×12段、制限時間60,000ms。
- 箱は1,000msごとに1個。赤・青・緑・灰の抽選確率は各25%。
- 最上段が箱で塞がれた列を除いて列を抽選し、生成可能な列がなければ終了する。
- 生成箱も支えを失った箱も、250msで1マス落下する。
- プレイヤーは上下左右。十字キーは移動／隣接箱のプッシュ、パンチは向き方向の隣接箱。
- プッシュは1個を1マスだけ。押し先の箱・盤面外を越えて動かさない。複数箱をまとめて押さない。
- 赤・青・緑のHPは1、灰は3。パンチ破壊と同色連結消去は別イベント。
- 消去は同色の上下左右の連結成分が3個以上。一直線限定ではなくL字も対象、斜めは対象外。
- 次の連結消去まで2,000ms以内ならコンボ。パンチ破壊だけで更新しない。
- 落下箱がプレイヤーのマスに入ったら即終了。原作画像にある×箱を追加しない。
- 灰色の連結消去、落下中の消去、プッシュ方向などの細部は`P-xx`も必ず読む。

## 技術・作業範囲

初期案はTypeScript＋Canvas 2D＋HTML/CSS、Vite、Vitest、Playwright。
既存リポジトリがある場合は、まず構成・指示・変更差分を確認し、理由なく作り直さない。
新規の場合はnpmを使い、実行環境に対応するバージョンを確認してlockfileを保存する。
ゲーム実行時の外部API・認証・DB・広告・解析サービス・有料サービスは不要。
重いゲームエンジン、React等のUI基盤、ネイティブアプリ化、PWA化は初期MVPで必須にしない。

- `src/core/`はDOM、Canvas、ブラウザ時計、`Math.random()`に直接依存させない。
- ゲーム時計・乱数・入力を外から渡し、seedと入力列で再現できる設計にする。
- 箱の正本は一つ。盤面配列と箱一覧を独立に更新して二重管理しない。
- タッチの複数指、指の解放、中断、ポーズを扱う。右手のパンチで左手の移動を解除しない。
- リトライ時は状態・タイマー・入力を初期化し、イベントリスナーやループを多重登録しない。
- `references/`は非配布の参考資料。原作画像・音源をゲームの`public/`やビルド成果物に含めない。
- 環境変数・秘密鍵・認証情報を要求しない。公開、課金、アカウント作成は行わない。

## 実装後に用意して実行するコマンド

以下は**現時点で存在するコマンドではない**。新規実装時に同等のscriptsを作る。
既存プロジェクトでは既存の同等コマンドを優先し、READMEに対応を記録する。

```sh
npm run typecheck
npm run lint
npm run test
npm run test:e2e
npm run build
```

`test`はwatchでない単発実行にする。必要なブラウザを導入できない等の制限は明記する。
テストを通すために確定ルールやアサーションを弱めない。実施していない検証を成功と書かない。
自動テストのWebKit／画面サイズエミュレーションを、iPhone実機Safari試験の代わりにしない。

## 完了時の報告

日本語で、変更内容、実行したコマンドと結果、未実施の検証、採用した仮仕様、残る不具合、
iPhoneでの起動方法を簡潔に報告する。テスト結果は実行ログに基づける。
人間が操作する実機試験は「未実施／要確認」を残してよいが、MVPの実機確認完了とは言わない。
